server:
  host: 0.0.0.0
  port: 8080
  static_directory: dist
  templates_directory: dist

log:
  level: info
  file_prefix: server
  directory: logs
  json: false

database:
  host: localhost
  port: 5432
  database: postgres
  user: postgres
  password: postgres

upload:
  max_size: 5000000000 # 5GB
  directory: data
